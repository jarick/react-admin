import styled from 'styled-components'

// language=SCSS prefix=dummy{ suffix=}
export const SortLink = styled.span`
  margin: 0 10px;
  color: #0FA0CE;
  text-decoration: underline;
  cursor: pointer;
`